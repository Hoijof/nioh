export enum types {
  FOOD,
  NIOH,
  OBSTACLE,
  HOSTILE,
}

export enum NIOH_STATES {
  IDLE,
  SEARCHING_FOOD,
}

export const MAX_HUNGER = 5000;
export const MAX_ACCELERATION = 50;
export const DEFAULT_VISION_RANGE = 50;
export const DEFAULT_VISION_WIDTH = Math.PI / 4;

export const DEBUG = true;
